# NSA技術ナビ 実装メモ（静的JSON化・詳細解説画面・候補リスト・検索機能）

前回の方針確認（Firebase外部DB化の妥当性検討）を踏まえ、以下の結論で実装を行った。

## 確定した方針

1. **Firebase等の動的DBは使わず、静的JSONで管理する。**
   Excelを1ファイルに統合し、そこから変換スクリプトでJSONを生成する運用に統一した。
2. **技術要素の詳細解説画面は、既存の「導入基準（a〜c）を見る」アコーディオンに統合する形で実装した。**
   （別ボタンを増やすのではなく、1つの「詳細解説を見る」ボタンの中にセクション分けして格納）
3. **その他提案（候補リスト機能／キーワード検索画面／データ更新日表示）も採用し実装した。**

## 1. Excelの統合

これまで別ファイルだった以下2つを、1つのマスターファイルにシート統合した。

- `NSAマッピング_改訂版_app.xlsx`（技術マッピング／導入検討ツール）
- `NSAマッピング_全項目解説_app.xlsx`（目次／技術解説一覧）

統合後：`NSAマッピング_マスター.xlsx`（プロジェクトのFilesセクションに要手動アップロード。理由は下記「制約事項」参照）

シート構成：
1. 目次（分野別）
2. 技術マッピング（52セル原本、4要素×13分野）
3. 導入検討ツール（199項目、a〜d基準）
4. 技術解説一覧（199項目、技術概要／期待される効果／導入課題／出典）

今後のデータ更新は、このマスターファイルの該当シートを直接編集する運用とする（2ファイル体制には戻さない）。

## 2. Excel→JSON変換スクリプト

`scripts/excel_to_json.py`（プロジェクトドキュメントに保存済み）

- マスターExcelの「導入検討ツール」と「技術解説一覧」をNo.で結合し、`tech_items.json`を生成する。
- 「導入検討ツール」のa〜c列（例：`中：普及員の技術研修・FFSファシリテーション能力`）はレベルと詳細説明に自動分割する。
- 「技術解説一覧」の出典欄（複数行・「ラベル: URL」形式）は`{label, url}`の配列に分解する。
- No.のずれ・タイトル不一致などがあれば標準エラー出力に警告を出す（実行時点では199件とも整合、警告0件を確認済み）。

使い方：
```
python3 excel_to_json.py NSAマッピング_マスター.xlsx tech_items.json
```

### JSONスキーマ概要

```json
{
  "generatedAt": "2026-09-14T16:41:53+09:00",
  "sourceFile": "NSAマッピング_マスター.xlsx",
  "itemCount": 199,
  "items": [
    {
      "id": 1, "cat": "分野横断：制度・基盤", "el": "A",
      "t": "介入項目名",
      "tl": "中", "td": "a.技術要素の詳細",
      "il": "大", "idt": "b.投資要素の詳細",
      "pl": "必須", "pd": "c.政策要素の詳細",
      "n": "d.その他（留意事項）",
      "overview": "技術概要",
      "effects": "期待される効果（4要素対応、自由文）",
      "sources": [{"label": "出典名", "url": "https://..."}]
    }
  ]
}
```

## 3. アプリ（`myNSA_demo` / `src/App.jsx`）の変更点

- **データ読み込みを静的JSON方式に変更**：`ITEMS`のハードコード配列を廃止し、`public/data/tech_items.json`を起動時にfetchする方式にした。ローディング表示・失敗時の再読み込みボタンを追加。
- **詳細解説アコーディオンの統合**：「詳細解説を見る」ボタン1つで、技術概要／期待される効果／前提条件（a〜c）／出典リンク（クリック可能）をまとめて表示する。効果テキストの4要素自動タグ付けは、元データの表記ゆれが大きく誤ラベリングのリスクがあるため今回は見送り、文章としてそのまま表示している（将来的にExcel側で構造化すれば対応可能）。
- **候補リスト機能**：各項目に「候補リストに追加」ボタンを追加。追加した項目は絞り込み・検索をまたいで蓄積され（`localStorage`に保存）、専用の「候補リスト」画面で一覧・コピー・印刷・全削除ができる。パッケージ組み立て作業を直接支援する狙い。
- **キーワード検索画面**：ヘッダーに「絞り込みで探す／キーワードで探す／候補リスト」のタブを追加。キーワード検索は技術名・概要のフリーワード検索で、4ステップの絞り込みとは独立に199項目全体から探せる。
- **データ更新日の表示**：JSONの`generatedAt`をヘッダーに表示。

## ファイル一覧（今回の成果物）

- `NSAマッピング_マスター.xlsx` … 統合後のマスターExcel（ユーザーに送付。プロジェクトのFilesへの再アップロードは手動でお願いします）
- `scripts/excel_to_json.py` … 変換スクリプト（プロジェクトドキュメントに保存済み）
- `tech_items.json` … 上記スクリプトで生成したデータ（ユーザーに送付。`myNSA_demo`リポジトリの`public/data/tech_items.json`に配置）
- `nsa_tech_navi.jsx` … 更新後のアプリ本体（プロジェクトドキュメントを更新済み。`myNSA_demo`リポジトリの`src/App.jsx`に反映してください）

## GitHubリポジトリへの反映方法

このセッションからは`myNSA_demo`リポジトリへの書き込み権限（GitHub認証）がないため、直接pushはしていない。読み取り専用でcloneしてコード変更・検証は行った。反映手順：

```
git clone https://github.com/snakada1615/myNSA_demo.git
cd myNSA_demo
# 1) src/App.jsx を今回のnsa_tech_navi.jsxの内容で置き換え
# 2) public/data/tech_items.json を配置
mkdir -p public/data
cp <ダウンロードしたtech_items.json> public/data/tech_items.json
# 3) 動作確認
npm install
npm run build
npm run preview
# 4) 問題なければコミット・push
git add src/App.jsx public/data/tech_items.json
git commit -m "静的JSON方式への移行、詳細解説/候補リスト/検索機能を追加"
git push
```

なお、変換スクリプト（`scripts/excel_to_json.py`）はリポジトリに含めておくと、次回以降Excel更新時に同じ手順でJSONを再生成できる。

## 制約事項（今回のセッションで検証できなかった点）

このクラウド作業環境はネットワークアクセスが制限されており、npmレジストリ（registry.npmjs.org）・PyPI・CDN等への外部アクセスがすべて拒否された（`gh` CLIも未認証）。そのため、`npm install` / `npm run build` による実機ビルド検証は今回実施できていない。代わりに、TypeScriptコンパイラ（`tsc --noEmit`）でJSXの構文チェックのみ行い、構文エラーがないことは確認済み。**ユーザー側の環境で`npm run build`を実行し、最終的な動作確認をお願いします。**

## 今後の検討候補

- 期待される効果（4要素対応）の自動タグ付け：Excel側で「主効果／副次効果」を構造化された列に分離すれば、画面上での色分けタグ表示が可能になる。
- 候補リストの共有：現状は`localStorage`（端末内保存）のみ。複数人での共有が必要になった場合は、その時点で外部保存先（Firebase等）の検討を再開するのが妥当。
